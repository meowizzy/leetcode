export class Solution {
  /**
   * @param {number[]} nums
   * @return {number[]}
   */
  productExceptSelf(nums: Array<number>) {
    const map = {};

    let index = 0;

    for (let i = 0; i < nums.length - 1; i++) {

    }
  }
}
